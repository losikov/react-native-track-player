<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.doublesymmetry.trackplayer">

    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="33" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application>

        <!-- The main service, handles playback, playlists and media buttons -->
        <service
            android:name="com.doublesymmetry.trackplayer.service.MusicService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Custom ContentProvider for exposing cached images to Android Auto -->
        <!-- Converts file:// URIs to content:// URIs for MediaBrowserService artwork -->
        <!-- The authority will be resolved at runtime using the host app's package name -->
        <!-- exported="true" is required for Android Auto to access images across process boundaries -->
        <!-- grantUriPermissions="true" allows Android Auto to read the images via content:// URIs -->
        <provider
            android:name="com.doublesymmetry.trackplayer.provider.ImageContentProvider"
            android:authorities="${applicationId}.trackplayer.imageprovider"
            android:exported="true"
            android:grantUriPermissions="true" />

    </application>

</manifest>
